export enum PageNameEnum {
  REDIRECT = "Redirect",
}

export enum PagePathEnum {
  HOME = "/home",
}

export enum RedirectType {
  NAME = "name",
  PATH = "path",
}

export enum RedirectField {
  // 重定向类型
  TYPE = "redirectType",
  PATH = "redirectPath",
}
