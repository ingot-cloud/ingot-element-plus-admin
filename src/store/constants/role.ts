export const moduleName = "role";

export enum Mutations {
  setRecords = "setRecords",
  openUpdateFlag = "openUpdateFlag"
}

export enum Getters {
  roleData = "roleData"
}

export enum Actions {
  fetchData = "fetchData",
  create = "create",
  remove = "remove",
  update = "update"
}
